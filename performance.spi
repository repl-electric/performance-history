load_support('piezoelectric_resonator'); use_bpm 127#; alive perc: 0, pitch: 0, kick: 0


def ping(n, *args)
  if n
    velocity = 20
    if args && args[0].is_a?(Numeric)
      vel = args[0]
      args = args[1..-1]
    end
    midi n, vel, *(args << {port: :iac_bus_1} << {channel: 1})
  end
end
def pong(n, *args)
  if n
    velocity = 20
    if args && args[0].is_a?(Numeric)
      vel = args[0]
      args = args[1..-1]
    end
    midi n, vel, *(args << {port: :iac_bus_1} << {channel: 2})
  end
end



live_loop :demo do
  tick
  midi_cc 10, (ring 1, 17).look, channel: 1
  ping :A3, sus: 4
  sleep 4/2.0
#  pong :c2, sus: 4
  sleep 4/2.0
end
